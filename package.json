{
  "name": "syncflow",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:local": "turbo run dev:local",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "workers:build": "turbo run build --filter=@syncflow/workers",
    "workers:dev": "npm run dev --workspace=@syncflow/workers",
    "workers:local": "npm run dev:local --workspace=@syncflow/workers",
    "api:dev": "npm run start:dev --workspace=api",
    "api:local": "npm run start:local --workspace=api",
    "web:dev": "npm run dev --workspace=web",
    "web:local": "npm run dev:local --workspace=web",
    "postinstall": "prisma generate --schema=apps/api/prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "turbo": "^2.0.0"
  }
}
